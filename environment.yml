name: recom_sys_combined
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python>=3.8
  - pytorch>=2.0.0
  - numpy>=1.24.0,<2.0.0
  - pandas>=2.0.0
  - scikit-learn>=1.3.0
  - scipy>=1.10.0
  - scikit-surprise>=1.1.3
  - implicit>=0.6.0
  - matplotlib>=3.7.0
  - pip
  - pip:
      - torch-geometric>=2.3.0  # Graph Neural Network library for PyTorch
      - tqdm>=4.65.0  # Progress bars for training
      - myfm  # Factorization Machines library (Bayesian FM with cross-platform support)
